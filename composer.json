{
    "name": "elephox/miniphox",
    "description": "A new Miniphox project.",
    "type": "project",
    "require": {
        "php": "^8.1",
        "elephox/miniphox-framework": "^0.4"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "serve": [
            "Config::disableProcessTimeout",
            "@php public/server.php"
        ]
    }
}
